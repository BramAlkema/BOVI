# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
coverage/
*.tsbuildinfo

# Generated pitch exports — the .md beside them is the source of truth.
# Deliberately narrow: a book PDF built into docs/ should NOT vanish silently.
docs/PITCH-*.docx
docs/PITCH-*.pdf

# Development
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
logs
*.log

# Cache
.npm
.yarn/cache
.cache

# Temporary files
tmp/
temp/
.tmp/

# Tooling / local agent state (never commit)
.claude/
.agent-os/
.gstack/

# Blender intermediates — all regenerable by re-running the generator script
# (blender --background --factory-startup --python blender/<script>.py -- anim).
# The shipped stills/clips are tracked at docs/book/figures/ instead.
blender/anim/
blender/anim_landscape/
blender/anim_tower/
blender/preview/
blender/preview_landscape/
blender/preview_sim/
blender/preview_tower/
blender/render/
blender/caption_*.png
blender/.caption.txt
*.blend1
